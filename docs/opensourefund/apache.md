[toc]

# Apache（2021/4/14）

## 有意义

### Accumulo（分布式KV存储）

基于Apache Hadoop，Apache Zookeeper, Apache Thrift 构建的，可靠的、可伸缩的、高性能的排序分布式KV数据存储系统。

### ActiveMQ（消息队列）

a fast and powerful Message Broker which supports many Cross Language Clients and Protocols and many advanced features.

### Airflow（调度和监控工作流）

workflow automation and scheduling that can be used to author and manage data pipelines.

### Ambari （Hadoop管理监控工具）

HDFS、MapReduce、YARN、Hive、Pig、 HBase、Zookeper、Sqoop和Hcatalog。

### Ant（构建工具）

a Java-based build tool.

### ApiSix（云原生的API服务网关）

Cloud-native microservices API gateway.

### Archiva（远程资源库maven等管理软件）

an extensible repository management software that helps taking care of your own personal or enterprise-wide build artifact repository. 

### Arrow（跨语言的内存数据格式）

A cross-language development platform for in-memory analytics.

### AsterixDB（大数据管理系统 (BDMS））

a scalable, open source Big Data Management System (BDMS).

### Atlas（大数据的元数据管理和数据治理平台）

Hadoop社区为解决Hadoop生态系统的元数据治理问题而产生的开源项目.

### Avro（行式序列化）

A data serialization system

### Axis2（Web服务的核心支援引擎）

a service hosting and consumption framework that makes it easy to use SOAP and Web Services.

### Bahir（Spark/Flink扩展）

extensions to multiple distributed analytic platforms, extending their reach with a diversity of streaming connectors and SQL data sources.

### Beam（统一编程模型的批流一体的数据处理）

An advanced unified programming model，Implement batch and streaming data processing jobs that run on any execution engine.

### BloodHound（项目的进展和缺陷跟踪）

a software development collaboration tool, including issue tracking, wiki and repository browsing.

### Bookkeeper（扩展、高可用、低延迟的存储服务）

A reliable replicated log service.

### Brooklyn（多数据中心的应用程序的蓝图和管理平台）

a framework for modeling, monitoring, and managing applications through autonomic blueprints.

### Brpc（百度的RPC开源）

an industrial-grade RPC framework for building reliable and high-performance services.

### Buildr（项目构建系统，类似Maven/Gradle）

a build system for Java-based applications, including support for Scala, Groovy and a growing number of JVM languages and tools.

### Bval（JavaBean数据验证）

an implementation of the Java Bean Validation Specfication (JSR303/346) , can work with Spring.

### Calcite （开源SQL解析工具）

般常用作集成到某些框架里，比如Apache Drill、Apache Hive、Apache Kylin、Apache Phoenix、Apache Samza、Apache Flink、Lingual等。

### CarbonData（索引列式数据格式）

an indexed columnar data format for fast analytics on big data platform, e.g. Apache Hadoop, Apache Spark, etc.

### Cassandra（NOSQL数据库）

 the right choice when you need scalability and high availability without compromising performance.

### Cayenne（Java ORM 框架）

User-friendly Java ORM with tools.

### CloudStack（高可用性及扩展性的云计算平台）

an IaaS (“Infrastracture as a Service”) cloud orchestration platform.

### Cocoon（Web开发框架）

Web development framework: separation of concerns, component-based.

### CouchDB（开源的面向文档的数据库管理系统）

**Seamless** multi-master sync, that scales from **Big Data** to **Mobile,** with an **Intuitive** HTTP/JSON API and designed for **Reliability.**

### Commons（常用的Java类库）

BeanUtils,  CLI，CodeC，Collections，Compress，Configuration，Crypto，CSV，IO，Lang，Math，Pool，Validator等。

### Cordova（跨平台的移动应用程序开发框架）

A tool to create cross-platform apps from standard web technologies (HTML, CSS, and JavaScript).

### Crail（分布式数据处理工作负载中快速共享临时数据）

a high-performance distributed data store designed for fast sharing of ephemeral data in distributed data processing workloads.

### Creadur Rat（检查软件许可证发行的准确性和高效性的工具）

improves accuracy and efficiency when reviewing and auditing releases.

### Creadur Tentacles（对上传到临时存储库的批量组件进行审计）

download all the archives from a staging repo, unpack them and create a little report of what is there.

### Creadur Whisker（应用程序对其发行版内容的许可特征建模。）

assists assembled applications to maintain correct legal documentation.

### CTakes（临床的文本分析和知识提取系统）

an open-source natural language processing system for information extraction from electronic medical record clinical free-text.

### Curator（Zookeeper的封装类库）

 A set of Java libraries that make using Apache ZooKeeper much easier.

### CXF（开源的Service框架）

An Open Source Service Framework.

### Daffodil（数据格式描述语言DFDL开源实现）

库，数据格式描述语言（Data Format Description Language）DFDL 1.0 规范的开源实现，以实现通用数据交换。

convert between fixed format data and XML/JSON based on a DFDL schema. 

### DataFu（Hadoop类库集合）

大规模数据处理的库函数，供Spark、MapReduce、Pig使用。

### DataLab（数据实验平台）

a platform for creating self-service, exploratory data science environments in the cloud using best-of-breed data science tools.

可选使用Zeeplin，Jupyter，Rstudio等分析工具。

### DataSketches（高性能大数据流算法库）

an open source, high-performance library of streaming algorithms commonly called "sketches" in the data sciences.

### DeltaSpike（Contexts & Dependency Injection Extensions）

a collection of portable Extensions for CDI Containers.

### Derby（关系型数据库，可内嵌）

an open source relational database implemented in Java.

### Directory&Fortress&Server&Studio（LDAP和Kerberos标准实现）

LDAP and Kerberos, entirely in Java.

### DolphinScheduler（分布式可视化DAG工作流任务调度系统）

a distributed and extensible workflow scheduler platform with powerful DAG visual interfaces.

### Doris（MPP数据仓库，OLAP）

a MPP-based interactive SQL data warehousing for reporting and analysis.

### Drill（分布式MPP查询系统）

a distributed MPP query layer that supports SQL and alternative query languages against NoSQL and Hadoop data storage systems. It was inspired in part by Google's Dremel.

### Druid（OLAP数据仓库）

a high performance real-time analytics database.

### Dubbo（Java RPC框架）

a high-performance, lightweight, java based RPC framework.

### ECharts（JS可视化库）

a charting and data visualization library written in JavaScript.

### Empire-db（关系型数据持久化组件，类似Hibernate/Mybatis）

a lightweight relational database abstraction layer and data persistence component.

### EventMesh（云原生基础服务，类似ServiceMesh）

a dynamic cloud-native basic service runtime used to decouple the application and middleware layer.

### Fineract（核心银行系统平台化建设）

Platform for Digital Financial Services.

### Flagon（测试平台）

an open-soure, thin-client behavioral logging capability useful for business analytics, usage analytics, usability and user testing. 

### Flex（跨平台的Web开发框架）

a highly productive, open source application framework for building and maintaining expressive web applications that deploy consistently on all major browsers, desktops and devices (including smartphones, tablets and tv).

### Flink（实时流处理系统）

Fast and reliable large-scale data processing

### Flume（日志收集系统）

a distributed, reliable, and available system for efficiently **collecting, aggregating and moving large amounts of log data** from many different sources to a centralized data store

### Fluo（Google搜索引擎的开源实现）

是 [Google Percolator](https://research.google.com/pubs/pub36726.html)（搜索索引）的开源实现，允许用户对存储在 Apache Accumulo 中的大型数据集进行增量更新，而无需重新处理所有的数据。与批处理和流处理框架不同的是，Fluo 提供了更低的延迟，并且可以在极大的数据集上运行。

### FreeMarker（模板引擎类库）

a template engine.

### Geode（缓存等数据管理平台，类似分布式缓存Redis）

Providing low latency, high concurrency data management solutions. 

内存数据库。

### Geronimo（JavaEE Web服务）

an open source server runtime that integrates the best open source projects to create Java/OSGi server runtimes that meet the needs of enterprise developers and system administrators. 

### Giraph（基于Hadoop的分布式迭代图处理系统）

灵感来自 BSP (bulk synchronous parallel) 和 Google 的 Pregel. 

### Gobblin（Hadoop 通用数据摄取框架）

A distributed data integration framework that simplifies common aspects of big data integration such as data ingestion, replication, organization and lifecycle management for both **streaming** and **batch** data ecosystems.

支持大部分流行的数据源，例如RDBMS（Oralce、Mysql、SqlServer）， Espresso，Kafka，RocksDB，S3，Salesforce和Google Analytics等。让数据收集工作变得更加简单和易用。

### Gora（ORM框架，为大数据提供内存数据模型与数据的持久化）

an in-memory data model and persistence for big data. 

支持对于列数据、key-value数据，文档数据与RDBMS数据的存储，还支持使用Apache Hadoop来对对大数据进行分析。

### Griffin（大数据的数据质量解决方案）

a data quality solution for big data, including both streaming and batch mode. It offers an unified process to measure data quality from different perspectives.

### Groovy（基于 JVM 平台的动态/敏捷编程语言）

A multi-faceted language for the Java platform.

### Guacamole（基于Web的远程桌面）

a **clientless remote desktop gateway**. It supports standard protocols like VNC, RDP, and SSH.

### Gump（持续集成工具）

 large scale continuous integration for various open source projects.

### Hadoop MapReduce（Hadoop计算框架）

map reduce framework.

### Hadoop HDFS（Hadoop分布式文件系统）

open-source software for reliable, scalable, distributed computing.

### Hadoop Yarn（Hadoop集群资源管理）

Yet another resource Negotiator.

### HAWQ（MPP的Hadoop SQL分析平台）

a Hadoop native SQL query engine that combines the key technological advantages of an MPP database with the scalability and convenience of Hadoop.

### HBase（NOSQL，KV数据库）

[Hadoop](https://hadoop.apache.org/) database, a distributed, scalable, big data store.

### Helix（集群管理框架，用于自动化的分区、复制和资源分布）

A cluster management framework for partitioned and replicated distributed resources

### Heron（分布式流处理）

A real-time, distributed, fault-tolerant stream processing engine.

### Hive（Hadoop数据仓库）

data warehouse software facilitates querying and managing large datasets residing in distributed storage.

### Hivemall（机器学习库，Hive/Spark/Pig）

a library for machine learning implemented as Hive UDFs/UDAFs/UDTFs.

### Hop（数据编排工具）

aims to facilitate all aspects of data and metadata orchestration.

### HttpServer（Web服务器）

an open-source HTTP server for modern operating systems including UNIX, Microsoft Windows, Mac OS/X and Netware.

### HttpComponents Client/Core（HTTP 协议的客户端/服务器编程工具包）

creating and maintaining a toolset of low level Java components focused on HTTP and associated protocols.

### Hudi（数据湖）

Hadoop Updates and Incrementals：ingests & manages storage of large analytical datasets over DFS (hdfs or cloud stores).

### Iceberg（表格式，数据湖）

managing huge analytic datasets using a standard at-rest table format that is designed for high performance and ease of use.

### Ignite（分布式数据库）

分布式数据库，支持以内存级的速度进行高性能计算。

### Impala（MPP SQL查询）

he open source, native analytic database for Apache Hadoop.

### IoTDB（物联网数据库，收集、存储、管理与分析）

an IoT native database with high performance for data management and analysis

### Isis（领域驱动应用的 java 框架）

a framework for rapidly developing domain-driven apps in Java.

### Ivy（项目依赖管理工具，与Ant集成）

a very powerful dependency manager oriented toward Java dependency management, even though it could be used to manage dependencies of any kind.

### Jackrabbit（Java内容仓库规范的实现）

JCR(Java Content Repository)，即Java内容仓库规范，定义了一套规范API，用于文件和元数据的存储管理、全文检索等。JBoss ModeShape、Apache Jackrabbit等都是JCR规范的具体实现

### JAMES（Java企业邮箱服务）

pure Java SMTP and POP3 Mail server and NNTP News server. 

### jclouds（云计算 Java 开发工具包）

an open source multi-cloud toolkit for the Java platform that gives you the freedom to create applications that are **portable across clouds** while giving you **full control** to use cloud-specific features.

### Jena（构建语义 Web 程序的 Java 框架）

A framework for developing Semantic Web and Linked Data applications in Java.

### JMeter（Web测试工具）

test performance both on static and dynamic resources (files, Servlets, Perl scripts, Java Objects, Data Bases and Queries, FTP Servers and more).

### Johnzon（解析和创建 JSONP 的 Java 库）

an implementation of JsonProcessing (aka JSR-353) and a set of useful extension for this specification like an Object mapper.

### Joshua（Java统计机器翻译平台）

statistical and other forms of machine translation.

### JSPWiki（Wiki服务）

A feature-rich and extensible WikiWiki engine built around the standard JEE components (Java, servlets, JSP).

### Juneau（编组 POJO和开发 REST微服务和 API）

编组 POJO（普通 Java 对象）和开发 REST（表征状态转移）微服务和 API。

### Kafka（分布式消息订阅-发布）

a distributed, fault tolerant, publish-subscribe messaging.

### Kerby（Java Kerberos）

Kerberos binding in Java.

### Kibble（可视化活动报告分析平台）

一个活动报告平台，用于收集、汇总、分析和可视化软件项目和社区中的活动。

### Knox（Hadoop 部署交互的应用网关）

通过REST API和UI与Apache Hadoop 部署交互的应用网关。

### Kudu（列式存储系统）

an open source distributed data storage engine that makes fast analytics on fast and changing data easy.

### Kylin（大数据分析型数据仓库）

Extreme OLAP Engine for Big Data.

### Libcloud（Python访问云计算服务的统一接口）

 a standard Python library that abstracts away differences among multiple cloud provider APIs.

### Liminal（机器学习编制平台）

an end-to-end platform for data engineers and scientists, allowing them to build, train and deploy machine learning models in a robust and agile way.

### Livy（基于*Apache* Spark的REST服务）

a REST interface for managing long running Apache Spark contexts in your cluster.

### Logging（日志记录）

logging services for C++， Java，Scala 等。

### Lucene / PyLucene（搜索引擎库/Python API）

a high-performance, full-featured text search engine library.

### MADlib（基于SQL的数据库内置的可扩展机的器学习库）

Scalable, Big Data, SQL-driven machine learning framework for Data Scientists.

目前支持PostgresSQL和GreenPlum。

### Mahout（大数据机器学习库）

a distributed linear algebra framework and mathematically expressive Scala DSL designed to let mathematicians, statisticians, and data scientists quickly implement their own algorithm.

### ManifoldCF（JAVA实现的爬虫框架）

Open-source software for transferring content between repositories or search indexes.

### Marvin-AI（机器学习操作的平台）

an open source Artificial Intelligence platform that focus on helping data science team members, in an easy way, to deliver complex solutions supported by a high-scale, low-latency, language agnostic and standardized architecture while simplifying the process of exploitation and modeling.

### Maven（项目开发/管理框架）

a project development management and comprehension tool.

### Maven Doxia（内容生成框架）

a content generation framework which aims to provide its users with powerful techniques for generating static and dynamic content.

### Mesos（集群资源管理框架）

a cluster manager that provides efficient resource isolation and sharing across distributed applications, or frameworks.

### MetaModel（统一的查询语言类库）

a common interface for discovery, exploration of metadata and querying of different types of data sources. 

Relational (JDBC) databases, CSV files, Excel spreadsheets, XML files, JSON files, Fixed width files, MongoDB, Apache CouchDB, Apache HBase, Apache Cassandra, ElasticSearch, OpenOffice.org databases, Salesforce.com, SugarCRM and even collections of plain old Java objects (POJOs).

### Milagro（分布式安全认证系统）

a crypto libraries and core security infrastructure for decentralized networks and distributed systems.

### Mina（网络应用框架）

A network application framework.

### Mina FtpServer（Ftp服务器，基于MINA）

 a 100% pure Java FTP server.

### Mina SSHD（Java SSH客户端和服务端，基于MINA）

a 100% pure java library to support the SSH protocols on both the client and server side.

### Mnemonic（非易失性混合内存存储的库，可与Spark集成）

a transparent nonvolatile hybrid memory oriented library for Big data, High-performance computing, and Analytics.

### MxNet（深度学习框架）

A truly open source deep learning framework suited for flexible research prototyping and production.

### Nemo（多样弹性部署的数据处理系统）

A Data Processing System for Flexible Employment With Different Deployment Characteristics.

可以作为Apache Beam的官方Runner运行。

### NiFi（数据流设计的可靠的数据处理和分发系统）

powerful and scalable directed graphs of data routing, transformation, and system mediation logic.

### NLPCraft（NLU应用的API）

An API to convert natural language into actions.

### Nutch（网络爬虫软件）

an open source web crawler software project.

可基于Hadoop集群运行。

### OFBiz（开源ERP解决方案）

an open source enterprise automation software project.

### Olingo（实现开放数据协议(OData)的Java库）

Apache Olingo serves client and server aspects of OData. It currently supports OData 2.0 and support OData 4.0 (beta).

### Oozie（Hadoop工作流调度）

A workflow scheduler system to manage Apache Hadoop jobs.

### OpenJPA（Java持久化实现）

 Java Persistence API Implementation.

### OpenMeetings（视频会议，及时消息）

Video conferencing, instant messaging, white board and collaborative document editing application.

### OpenNLP（NLP工具类）

a machine learning based toolkit for the processing of natural language text.

### OpenOffice（似于微软MS Office软件跨平台的办公室软件）

An open-source, office-document productivity suite.

### OpenWebBeans（JavaEE依赖注入）

 Dependency Injection Platform. 

### OpenWhisk（分布式无服务器平台，开源FaaS平台）

a platform for building serverless applications with functions. 

### ORC（列式存储格式）

ORC is a self-describing type-aware columnar file format designed for Hadoop workloads. It is optimized for large streaming reads, but with integrated support for finding required rows quickly.

### Ozone（Hadoop云原生的分布式对象存储）

a distributed object store for Hadoop-based and Cloud-native environments.

### PageSpeed（网页性能只能优化）

a series of open source technologies to help make the web faster by rewriting web pages to reduce latency and bandwidth.

### Parquet（列式存储格式）

Apache Parquet is a general-purpose columnar storage format, built for Hadoop, usable with any choice of data processing framework, data model, or programming language.

### PDFBox（Java处理PDF的类库）

an open source Java tool for working with PDF documents.

### Pegasus（分布式KV存储系统）

a distributed key-value storage system which is designed to be simple, horizontally scalable, strongly consistent and high-performance.

### Pinot（实时分布式的 OLAP 数据存储和分析系统）

a distributed columnar storage engine that can ingest data in real-time and serve analytical queries at low latency.

### Pivot（构建可安装的网络应用）

an open-source platform for building installable Internet applications (IIAs).

### Phoenix（SQL on HBase）

High performance relational database layer over Apache HBase for low latency applications.

### Pig（大数据分析系统）

a platform for analyzing large data sets on Hadoop.

### PLC4X（可编程逻辑控制器的统一API）

Universal API for communicating with programmable logic controllers.

### POI（MS Office编程的Java API）

Java API To Access Microsoft Document File Formats.

### Pony Mail（邮箱归档、查看等交互服务）

 a mail-archiving, archive viewing, and interaction service, that can be integrated with many email platforms.

### Portable Runtime（跨平台库）

 Cross-platform library easing many progamming tasks in C.

### Portals（协作的软件开发项目，提供门户网站相关软件）

a robust, full-featured, commercial-quality, and freely available Portal related software on a wide variety of platforms and programming languages.

### Pulsar（分布式发布-订阅消息系统）

highly scalable, low latency messaging platform running on commodity hardware.

### Qpid（Advanced Message Queuing Protocol（AMQP）企业消息实现）

AMQP enterprise messaging implementation.

### Ranger（Hadoop数据权限监控管理平台）

enable, monitor and manage comprehensive data security across the Hadoop platform.

### Ratis（可自定制的Java Raft实现）

a highly customizable Raft protocol implementation in Java.

### REEF（可保留评估程序执行框架，Yarn/Mesos上开发可移植应用程序的库）

Retainable Evaluator Execution Framework：a scale-out computing fabric that eases the development of Big Data applications on top of resource managers such as Apache YARN and Mesos.

### River（JINI标准实现）

a standards-compilani JINI service.

### Rivet（Tcl 语义的Web框架）

a system for creating dynamic web content via the Tcl programming language integrated with Apache Web Server. 

### RocketMQ（消息队列）

a fast, low latency, reliable, scalable, distributed, easy to use message-oriented middleware, especially for processing large amounts of streaming data.

### Roller（Java版本的WebBlog系统）

a full-featured, multi-user and group blog server suitable for blog sites large and small.

### Royale（开源前端应用程序技术，MXML & AS3 ）

improving developer productivity in creating applications for wherever Javascript runs (and other runtimes).

### Rya（RDF数据的存储、获取和分析）

scalable storage, retrieval, and analysis of RDF data

### Samza（分布式流处理框架）

build stateful applications that process data in real-time from multiple sources including Apache Kafka.

### Sandesha2（WS-RM的实现）

an Axis2 module implementing WS-RM.

### Santuario（ XML数字签名和加解密规范的实现）

Library implementing XML Digital Signature Specification & XML Encryption Specification.

### Scout（JAXR的实现）

an implementation of the JSR 93 (JAXR).

### SDAP（科学数据分析平台）

an integrated data analytic center for Big Science problems.

SDAP是一个技术软件解决方案，旨在更好地使科学家参与推进地球物理海洋学的研究。

### Sedona（大规模空间数据的集群计算系统，基于Spark）

a big geospatial data processing engine.

### Serf（基于APR的C的HTTP Client库）

High performance C-based HTTP client library built upon the Apache Portable Runtime (APR) library.

### ServiceComb（微服务框架，对标SpringCloud）

a microservice framework that provides a set of tools and components to make development and deployment of cloud applications easier.

### ServiceMix（开源ESB,适合SOA项目的集成）

 an open source ESB providing routing and mediation features.

### ShardingSphere（分布式数据库中间件解决方案）

a database clustering system providing data sharding, distributed transactions, and distributed database management

### Shiro（Java安全框架）

a powerful and flexible open-source security framework that cleanly handles authentication, authorization, enterprise session management, single sign-on and cryptography services.

### SINGO（分布式深度学习平台）

a distributed deep learning platform.

### SkyWalking（Java分布式应用追踪系统）

the application performance management and monitoring (APM) tool.

尤其是针对微服务、云原生和面向容器的分布式系统架构。

### Sling（Web内容存储框架）

a web framework that uses a Java Content Repository, such as Apache Jackrabbit, to store and manage content. 

### Solr（全文搜索服务，基于Lucene）

an open source enterprise search server based on the Lucene Java search library.

### SpamAssassin（识别垃圾邮件的系统）

an extensible email filter which is used to identify spam.

### Spark（大数据处理系统）

 a fast and general engine for large-scale data processing.

### Spatial Information System（空间信息管理系统）

provides  data structures for geographic data and associated metadata along with methods to manipulate those data structures. 

### Sqoop（Hadoop和RDBMS体系之间传送数据）。

a tool designed for efficiently transferring bulk data between Apache Hadoop and structured datastores such as relational databases.

### Steve（Python投票系统）

a collection of online voting tools, used by the ASF, to handle STV and other voting methods.

### Storm（分布式实时计算系统）

a distributed real-time computation system, provides a set of general primitives for doing real-time computation.

### StreamPipes（企业IoT工具箱，用于连接、分析、探索IoT数据）

a self-service (Industrial) IoT toolbox to enable non-technical users to connect, analyze and explore (Industrial) IoT data streams.

### Streams（实现ActivityStreams 技术的轻量级框架）

interoperability of online profiles and activity feeds.

### Submarine（云原生的机器学习平台）

allowing infrastructure engineers and data scientists to build deep learning applications (TensorFlow, PyTorch, etc.) 

### Subversion（开源的集中式版本控制系统）

Enterprise-class centralized version control for the masses.

### Superset（数据可视化平台）

data exploration, analysis, visualization, and dashboarding.

### Synapse（XML 和 Web 服务管理与集成代理）

a lightweight ESB engine and XML router.

### Syncope（JAVA EE 开发的开源的适用于企业的数字身份管理系统）

an Open Source system for managing digital identities in enterprise environments.

### SystemDS（原SystemML，大数据的机器学习平台）

A machine learning platform optimal for big data.

### Tapestry（面向组件的JavaWeb应用框架）

Component oriented framework for creating dynamic, robust, highly scalable web applications in Java.

### Teaclave（通用的安全计算框架）

a universal secure computing platform.

### Tez（基于Yarn的支持 DAG 作业的开源计算框架）

building an application framework which allows for a complex directed-acyclic-graph of tasks for processing data.

### Thrift（RPC框架）

a framework for scalable cross-language services development, combines a software stack with a code generation engine to build services.

### Tika（内容抽取的工具集合）

detects and extracts metadata and structured text content from various documents using existing parser libraries.

检测**上千种文件类型**，并提取它们的**元数据和文本**。

### TinkerPop（图OLAP和OLTP系统）

A graph computing framework for both graph databases (OLTP) and graph analytic systems (OLAP).

### Tomcat（Web Server）

A Java Servlet, JavaServer Pages, Java Expression Language and Java WebSocket specifications implementation.

### TomEE（J2EE 6 认证的 Tomcat 企业版本）

an all-Apache Java EE 6 Web Profile stack for Apache Tomcat.

### Toree（Apache Spark远程Restful交互）

provides applications with a mechanism to interactively and remotely access Apache Spark.

### Torque（Java的对象关系映射）

an object-relational mapper for Java.

### Traffic Control（分布式、可扩展的冗余解决方案，可用于构建CDN）

building, monitoring, configuring, and provisioning a large scale content delivery network (CDN).

### Traffic Server（HTTP缓存代理服务器）

an HTTP proxy cache.

### Trafodion（基于Hadoop平台的交易型分布式SQL引擎）

Webscale SQL-on-Hadoop solution enabling transactional or operational workloads on Apache Hadoop.

### Training（以开源的形式为各种项目创建高质量的培训材料）

develop resources which can be used for training purposes in various media formats, languages and for various Apache and non-Apache target projects.

### TubeMQ（消息队列）

distributed messaging queue (MQ) system.

### Turbine（构建Web应用）

a servlet based framework that allows Java developers to quickly build web applications.

### Tuweni（开发区块链和反中心化的Java库）

a set of libraries and other tools to aid development of blockchain and other decentralized software in Java and other JVM languages.

### TVM（端到端的深度学习编译框架）

compilation of machine learning models to run on a wide range of hardware platforms.

可重复使用的工具链来将高级神经网络描述从深度学习框架前端向下编译为多硬件后端的低级机器代码。

### UIMA（非结构化信息管理应用）

Annotator components and a scalable integration and deployment framework for Unstructured Information analysis

### Unomi（Java开源客户数据平台CDP,Customer Data Platform）

providing a reference implementation of the OASIS Customer Data Platform specification currently being worked on by the OASIS Context Server Technical Committee.

### Usergrid（Baas平台）

The BaaS Framework *you* run.

### VCL（计算资源管理平台）

A cloud computing platform for provisioning and brokering access to dedicated remote compute resources.

自服务的系统，为最终用户提供专用计算环境的远程访问环境。

### Velocity（基于Java的模板引擎）

a Java-based template engine. It permits anyone to use a simple yet powerful template language to reference objects defined in Java code.

### Velocity Texen（基于Velocity和Ant的代码生成）

a general purpose text generating utility based on Apache Velocity and Apache Ant.

### Velocity DVSL（Declarative Velocity Style Language）

A tool modeled after XSLT and intended for general XML transformations using the Velocity Template Language.

### Weex（跨平台的高性能UI框架）

a framework for building Mobile cross-platform high performance UI.

### Wicket（Java Web应用框架）

an open source Java component oriented web application framework.

### WS Axiom（XML解析）

An XML and SOAP object model which supports deferred parsing and on-demand building of the object tree.

### WS Woden（WSDL文档处理库）

a Java API for reading, manipulating, creating and writing WSDL documents.

### Xalan（XSLT处理的库）

XSLT的处理包，用于将XML文档转为HTML文档,文本文件TXT,或其他格式的XML文档。

### Xerces（XML解析库）

a validating XML parser.

### XMLBeans（XML和Java映射）

technology for accessing XML by binding it to Java types. 

### XMLGraphics Batik （SVG操作的Java库）

a Java-based toolkit for applications or applets to use images in the Scalable Vector Graphics (SVG) format for various purposes, such as display, generation and manipulation.

### XMLGraphics FOP（XSL格式对象实现）

XSL Formatting Objects (XSL-FO) implementation.

### Yetus（系统管理工具，为软件项目提供贡献和发布流程）

 A collection of libraries and tools that enable contribution and release processes for software projects.

### YuniKorn（用于服务和批处理工作负载的增强型Kubernetes调度程序）

a standalone resource scheduler responsible for scheduling batch jobs and long-running services on large scale distributed systems running in on-premises environments as well as different public clouds.

### Zepplin（基于Web的交互式NoteBook）

a modern web-based tool for the data scientists to collaborate over large-scale data exploration and visualization projects.

### Zookeeper（高可用的分布式协调系统）

 an effort to develop and maintain an open-source server which enables highly reliable distributed coordination.



## No

### Allura（基于 Web 的软件工具和协作软件开发的平台）

an open source implementation of a software "forge", a web site that manages source code repositories, bug reports, discussions, wiki pages, blogs and more for any number of individual projects.

### Airavata（管理分布式系统软件？）

a software framework that enables you to compose, manage, execute, and monitor large scale applications and workflows on distributed computing resources such as local clusters, supercomputers, computational grids, and computing clouds.

### Aries（企业OSGi应用编程模型）

delivering a set of pluggable Java components enabling an enterprise OSGi application programming model.

### Any23（结构化数据抽取库）

a library, a web service and a command line tool that extracts structured data in RDF format from a variety of Web documents.

### Bigtop（Hadoop及其周边生态进行打包，分发和测试的工具）

for Infrastructure Engineers and Data Scientists looking for comprehensive packaging, testing, and configuration of the leading open source big data components.

### Camel（基于规则的路由以及媒介引擎）

an Open Source integration framework that empowers you to quickly and easily integrate various systems consuming or producing data.

一个集成框架，旨在使项目集成更加高效。

### Chemistry（内容管理互操作服务）

open source implementations of the Content Management Interoperability Services (CMIS) specification.

### Clerezza（OSGi RESTFul Semantic Web应用和服务）

a service platform which provides a set of functionality for management of semantically linked data accessible through RESTful Web Services and in a secured way.

### Felix（OSGi版本4规范的实现）

OSGi framework implementation and related technologies.

### Karaf（osgi的运行环境，发布和运行osgi服务的容器）

 an OSGi distribution for server-side applications.

### Mynewt（资源受限嵌入式环境的操作系统）

embedded OS optimized for networking and built for remote management of constrained devices.

### MyFaces（JSF Web框架实现）

free open source implementation of Jakarta Server Faces, a web application framework that accomplishes the MVC paradigm. 

### MyFaces Tobago（创建具有统一外观的WEB应用的开发程序）

provide the community with a well designed set of user interface components based on JSF.

### NuttX（实时嵌入型操作系统）

 a mature, real-time embedded operating system (RTOS).

### OODT（面向对象数据技术）

Apache OODT 是软件框架，也是快速构建科学数据系统的架构风格。它提供用于数据捕捉、综合处理、元数据提取、工作流管理、资源管理和数据处理的组件。

### Petri（评估、教育、采纳基金会的合作发展政策及程序，以及加入基金会的利弊。）

deals with the assessment of, education in, and adoption of the Foundation's policies and procedures for collaborative development and the pros and cons of joining the Foundation.

### Struts（Java EE Web应用的MVC设计模式的应用框架）

 a free open-source framework for creating Java web applications.

### Vysper（模块化的,全功能的 XMPP(jabber)服务器软件，基于Mina）

A modular, full featured XMPP (Jabber) server.

### Whimsy（显示和可视化与ASF组织和流程相关的各种数据）

Tools that display and visualize various bits of data related to ASF organizations and processes.